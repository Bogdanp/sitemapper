# Sitemapper

```
Usage of ./sitemapper:
  -concurrency int
        The maximum number of concurrent requests to run. (default 8)
  -start string
        The URL to start crawling. (default "http://example.com")
  -verbose
        Print debug info.
```
